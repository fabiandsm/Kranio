name: CI - Validación DAGs Airflow

on:
  push:
    branches: ["main", "feature-*"]
  pull_request:
    branches: ["main"]

jobs:
  validate-dags:
    runs-on: ubuntu-latest

    steps:
      # ----------------------------------------------------
      # Checkout del repositorio
      # ----------------------------------------------------
      - name: Checkout repo
        uses: actions/checkout@v4

      # ----------------------------------------------------
      # Python
      # ----------------------------------------------------
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      # ----------------------------------------------------
      # Instalar Airflow
      # ----------------------------------------------------
      - name: Install Apache Airflow
        run: |
          python -m pip install --upgrade pip

          AIRFLOW_VERSION=2.8.1
          PYTHON_VERSION=3.10
          CONSTRAINT_URL="https://raw.githubusercontent.com/apache/airflow/constraints-${AIRFLOW_VERSION}/constraints-${PYTHON_VERSION}.txt"

          pip install "apache-airflow==${AIRFLOW_VERSION}" --constraint "${CONSTRAINT_URL}"

      # ----------------------------------------------------
      # Instalar dependencias DAG
      # ----------------------------------------------------
      - name: Install DAG dependencies
        run: |
          echo "Instalando dependencias base..."
          pip install pandas numpy requests

          if [ -f automatizacion_pipeline/requirements.txt ]; then
            echo "Instalando requirements del pipeline..."
            pip install -r automatizacion_pipeline/requirements.txt
          else
            echo "No se encontró requirements.txt, se continúa."
          fi

      # ----------------------------------------------------
      # Configurar AIRFLOW_HOME
      # ----------------------------------------------------
      - name: Set AIRFLOW_HOME
        run: echo "AIRFLOW_HOME=${{ github.workspace }}/airflow" >> $GITHUB_ENV

      # ----------------------------------------------------
      # Crear estructura airflow
      # ----------------------------------------------------
      - name: Create airflow folders
        run: |
          mkdir -p $AIRFLOW_HOME/dags

          if [ -d automatizacion_pipeline ]; then
            cp automatizacion_pipeline/*.py $AIRFLOW_HOME/dags || true
          else
            echo "Carpeta automatizacion_pipeline no encontrada"
          fi

      # ----------------------------------------------------
      # Validar DAGs
      # ----------------------------------------------------
      - name: Validate DAGs (import test)
        run: |
          python - <<EOF
          from airflow.models import DagBag

          dag_folder = "${{ github.workspace }}/automatizacion_pipeline"
          print(f"Validando DAGs en: {dag_folder}")

          dagbag = DagBag(
              dag_folder=dag_folder,
              include_examples=False
          )

          if dagbag.import_errors:
              print("❌ Errores al importar DAGs:")
              for dag, err in dagbag.import_errors.items():
                  print(f"{dag}: {err}")
              raise SystemExit(1)

          print(f"✅ DAGs cargados correctamente: {len(dagbag.dags)}")
          EOF
